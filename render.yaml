services:
  - type: web
    name: multirefage
    runtime: python
    buildCommand: |
      pip install -r app/requirements.txt
    startCommand: |
      uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.12
    plan: free